import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer>
      <div className={`${styles.footer_container} container`}>Footer</div>
    </footer>
  )
}

export default Footer